[project]
name = "evolith-workspace"
version = "0.1.0"
description = "Evolith Knowledge OS Workspace"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
    "apps/api",
    "packages/*"
]

[tool.vercel]
entrypoint = "apps.api.main:app"
