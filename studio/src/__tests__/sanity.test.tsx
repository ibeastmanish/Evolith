import { describe, it, expect } from 'vitest';

// Basic sanity check to ensure testing environment is valid
describe('Sanity Check', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});
